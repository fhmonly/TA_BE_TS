{
    "version": 2,
    "builds": [{
        "src": "/bin/build/server.js",
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/bin/build/server.js"
    }]
}